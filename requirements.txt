# Install python3-qpid-proton via apt (not on PyPI):
#   Ubuntu/Debian: sudo apt-get install python3-qpid-proton
#   Container: see Containerfile
numpy>=1.24
torch>=2.0
torchaudio>=2.0
faster-whisper>=1.0
